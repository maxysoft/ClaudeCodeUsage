/**
 * Provider-copy and selection contracts shared by the unified dashboard.
 *
 * Codex HTML is rendered by UsageWebviewProvider in webview.ts. This module
 * intentionally exports no provider-specific page renderer.
 */
export {
  CODEX_COPY_EN,
  CodexViewCopy,
  defaultDashboardProvider,
} from './codexViewComponents';
