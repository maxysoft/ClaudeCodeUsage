# V2.2.1 刷新降载与匿名诊断 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改变默认刷新体验和 Claude 用量结果的前提下，移除 active 8 秒轮询覆盖，加入最长 300 秒的 watcher quiet-debounce、匿名阶段诊断，以及流式且最多 8 路并发的首时间戳读取，作为 issue #70 在 V2.2.1 的低风险缓解。

**已批准设计：** [V2.2.1 稳定化设计（简体中文）](../specs/2026-07-17-v2.2.1-stabilization-design.zh-CN.md)

**Architecture:** 把纯刷新策略、JSONL manifest/排序、诊断格式化从 VS Code 生命周期代码中拆出，使它们可由 `node:test` 直接验证。每次刷新只扫描一次 manifest，并把同一 manifest 传给 loader；poll 始终使用用户的 `refreshInterval`，watcher 只负责 quiet debounce，但仍更新 activity 时间供 quota TTL 使用。loader 返回纯数字诊断，由 extension 补齐 trigger、聚合/渲染耗时及 watcher/coalescing 计数后写入 OutputChannel。

**Tech Stack:** TypeScript 5.8、Node.js 20+ 标准库（`node:fs`、`node:string_decoder`、`node:test`）、VS Code Extension API；不增加运行时依赖。

## Global Constraints

- **前置条件：** 必须先执行并合入 `docs/superpowers/plans/2026-07-17-codex-takeover-hygiene.zh-CN.md`；本计划依赖其中已经建立的唯一 `## [2.2.1] — Unreleased` CHANGELOG 标题、package ignore 审计和 Codex 维护约定。
- 目标分支为 `codex/v2.2.1-stabilization`，基线为已发布的 `v2.2.0`。
- V2.2.1 仍然只读取 Claude Code；本计划不加入 Codex provider。
- 不增加任何 runtime dependency。
- `refreshInterval` 的合法范围仍是 30–3600 秒；poll 不得在 active 状态下缩短为 8 秒。
- `fileWatchSeconds` 只表示 quiet debounce；保留默认值 `2`，并新增 `60`、`120`、`300` 秒选项。
- 诊断日志不得包含 prompt、绝对路径、session ID、credential 或原始 JSONL 行。
- 精确用量与估算 insight 的口径均不得变化。
- 新设置保留当前默认行为；不得静默迁移既有用户的刷新节奏。
- 新增 UI 文案必须覆盖现有七个 locale；秒数枚举标签沿用 `settings.ts` 已有的技术英文约定。
- README 改动必须同步六份：`README.md`、`README-en.md`、`README-zh-TW.md`、`README-zh-CN.md`、`README-ja.md`、`README-ko.md`。
- 每项行为变化先 RED、再 GREEN；交付前 `npm test` 必须零失败。
- 只有 prerelease 在 Linux 复现者机器上验证后，才能声称 #70 已解决；否则发布说明只能称为 mitigation，并保持 #70 开放。

### Codex Desktop 命令环境

Codex Desktop 的非交互 shell 可能没有 `node`、`npm`、`npx`。计划中的 `npm run compile` / `npm test` 是 CI 与普通开发机的规范命令；在当前 Codex 工作区必须使用下面的固定等价命令，不得临时下载未锁定版本：

```bash
CCU_NODE=/Users/carl/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
"$CCU_NODE" node_modules/typescript/bin/tsc -p ./
"$CCU_NODE" --test out/test/*.test.js
"$CCU_NODE" node_modules/@vscode/vsce/vsce ls --no-dependencies
```

每个执行步骤都视为新的 shell；凡后文单独出现 `"$CCU_NODE"`，必须在同一个 code block/command 前重新赋值为上述绝对路径。

---

## 文件职责

- Create: `src/refreshPolicy.ts` — poll 间隔、live-refresh 枚举、并发触发合并优先级，全部为纯函数。
- Create: `src/claudeUsageFiles.ts` — JSONL 发现/stat manifest、manifest diff、流式首时间戳读取和上限为 8 的并发排序。
- Create: `src/refreshDiagnostics.ts` — 只接受匿名数字的诊断类型和确定性日志格式化。
- Create: `src/test/refreshPolicy.test.ts` — 刷新 cadence、quiet-debounce 选项和 pending trigger 合并测试。
- Create: `src/test/claudeUsageFiles.test.ts` — manifest、流式读取和并发上限测试。
- Create: `src/test/refreshDiagnostics.test.ts` — 匿名诊断输出契约测试。
- Modify: `src/settings.ts:367-417` — `fileWatchSeconds` 新选项。
- Modify: `src/i18n.ts:1745-1970` — 七个现有 locale 的 quiet-debounce 帮助文字（英语在 `settings.ts`，其余六种在此文件）。
- Modify: `src/types.ts:265-320` — `ExtensionConfig` 保持相同字段，补充 quiet-debounce 语义注释。
- Modify: `src/dataLoader.ts:1-69,522-876,975-1014,2843-2872` — 复用已扫描 manifest、移除全文件首时间戳 pass、返回 I/O 诊断。
- Modify: `src/extension.ts:39-97,120-127,670-954,1040-1204` — 删除 active cadence，传递 refresh trigger，收集 watcher/coalescing 和阶段耗时。
- Modify: `CHANGELOG.md:7` — 新增 V2.2.1 mitigation 条目，不宣称 root fix。
- Modify: `README.md:302-305`、`README-en.md:63-71`、`README-zh-TW.md`、`README-zh-CN.md:175-176`、`README-ja.md`、`README-ko.md` — 写入 #70 临时 workaround。

### Task 1: 用纯函数锁定 poll 与 quiet-debounce 语义

**Files:**
- Create: `src/refreshPolicy.ts`
- Create: `src/test/refreshPolicy.test.ts`
- Modify: `src/settings.ts:378-397`
- Modify: `src/i18n.ts:1745-1970`
- Modify: `src/types.ts:265-320`
- Modify: `src/extension.ts:85-94,899-954`

**Interfaces:**
- Consumes: `ExtensionConfig.refreshInterval: number`、`ExtensionConfig.fileWatchSeconds: number`。
- Produces: `LIVE_REFRESH_SECONDS`、`pollIntervalMs(refreshIntervalSeconds: number): number`、`RefreshTrigger`、`mergeRefreshTrigger(current: RefreshTrigger | null, incoming: RefreshTrigger): RefreshTrigger`。

- [ ] **Step 1: 写出会失败的 cadence 与枚举测试**

```ts
// src/test/refreshPolicy.test.ts
import { test } from 'node:test';
import * as assert from 'node:assert/strict';
import {
  LIVE_REFRESH_SECONDS,
  mergeRefreshTrigger,
  pollIntervalMs,
} from '../refreshPolicy';

test('poll interval always honors refreshInterval and never applies an active override', () => {
  assert.equal(pollIntervalMs(30), 30_000);
  assert.equal(pollIntervalMs(60), 60_000);
  assert.equal(pollIntervalMs(300), 300_000);
  assert.equal(pollIntervalMs(900), 900_000);
  assert.equal(pollIntervalMs(3600), 3_600_000);
});

test('live refresh keeps the 2-second default choices and adds long quiet delays', () => {
  assert.deepEqual(LIVE_REFRESH_SECONDS, ['0', '1', '2', '5', '10', '20', '30', '60', '120', '300']);
});

test('coalescing retains the strongest pending trigger', () => {
  assert.equal(mergeRefreshTrigger(null, 'poll'), 'poll');
  assert.equal(mergeRefreshTrigger('poll', 'watch'), 'watch');
  assert.equal(mergeRefreshTrigger('watch', 'focus'), 'focus');
  assert.equal(mergeRefreshTrigger('settings', 'manual'), 'manual');
  assert.equal(mergeRefreshTrigger('manual', 'poll'), 'manual');
});
```

- [ ] **Step 2: 运行 RED，确认缺少纯策略模块**

Run: `npm run compile`

Expected: FAIL，包含 `TS2307: Cannot find module '../refreshPolicy'`。

- [ ] **Step 3: 实现纯策略并让设置表复用同一组选项**

```ts
// src/refreshPolicy.ts
export const LIVE_REFRESH_SECONDS = [
  '0', '1', '2', '5', '10', '20', '30', '60', '120', '300',
] as const;

export type RefreshTrigger =
  | 'startup'
  | 'poll'
  | 'credentials'
  | 'watch'
  | 'focus'
  | 'workspace'
  | 'settings'
  | 'pricing'
  | 'manual';

const TRIGGER_PRIORITY: Record<RefreshTrigger, number> = {
  startup: 0,
  poll: 1,
  credentials: 2,
  watch: 3,
  focus: 4,
  workspace: 5,
  settings: 6,
  pricing: 7,
  manual: 8,
};

export function pollIntervalMs(refreshIntervalSeconds: number): number {
  const seconds = Number.isFinite(refreshIntervalSeconds)
    ? Math.min(3600, Math.max(30, refreshIntervalSeconds))
    : 60;
  return seconds * 1000;
}

export function mergeRefreshTrigger(
  current: RefreshTrigger | null,
  incoming: RefreshTrigger
): RefreshTrigger {
  if (current === null) return incoming;
  return TRIGGER_PRIORITY[incoming] > TRIGGER_PRIORITY[current] ? incoming : current;
}
```

```ts
// src/settings.ts — replace only fileWatchSeconds.enumValues/enumLabels
enumValues: [...LIVE_REFRESH_SECONDS],
enumLabels: ['Off', '1s', '2s', '5s', '10s', '20s', '30s', '60s', '120s', '300s'],
```

在 `src/settings.ts` 顶部加入：

```ts
import { LIVE_REFRESH_SECONDS } from './refreshPolicy';
```

把 `src/types.ts` 中 `fileWatchSeconds` 的注释改成明确契约：

```ts
// Quiet period after the last JSONL watcher event; 0 disables watching.
fileWatchSeconds: number;
```

同时把设置帮助明确为 quiet debounce。英语在 `src/settings.ts` 使用下面的完整文字；`src/i18n.ts` 的德语、繁中、简中、日语、韩语、葡萄牙语条目分别替换为对应文字，七个 locale 都必须说明“最后一次事件后等待”，不能继续写成模糊的“新活动后多久刷新”：

```ts
// src/settings.ts
help: 'Wait after the last local JSONL change before refreshing (quiet debounce; each new event restarts the delay). No API call is made; quota fetches are throttled separately. Off disables watching, and 60–300s is the lowest-CPU option for large histories.',
```

```ts
// src/i18n.ts — exact SETTINGS_I18N fileWatchSeconds.help replacements
de: 'Wartezeit nach der letzten lokalen JSONL-Änderung vor der Aktualisierung (Ruhe-Debounce; jedes neue Ereignis startet die Wartezeit neu). Es erfolgt kein API-Aufruf; Kontingent-Abfragen werden separat gedrosselt. „Aus“ deaktiviert die Überwachung, 60–300 s schont große Verläufe am stärksten.'
zh-TW: '在最後一次本機 JSONL 變更後等待多久才重新整理（安靜期去抖；每個新事件都會重新計時）。不會呼叫 API；配額查詢另行節流。「關閉」會停用監控，60–300 秒最適合降低大量歷史資料的 CPU 負載。'
zh-CN: '在最后一次本地 JSONL 变更后等待多久再刷新（静默期防抖；每个新事件都会重新计时）。不会调用 API；配额查询单独节流。“关闭”会停用监控，60–300 秒最适合降低大历史的 CPU 负载。'
ja: '最後のローカル JSONL 変更から更新まで待つ時間です（quiet debounce。新しいイベントごとに待ち時間をリセット）。API は呼び出さず、クォータ取得は別に抑制されます。Off で監視を無効化し、大きな履歴では 60～300 秒が最も CPU 負荷を抑えます。'
ko: '마지막 로컬 JSONL 변경 후 새로 고침까지 기다리는 시간입니다(quiet debounce, 새 이벤트마다 대기 시간이 다시 시작됨). API를 호출하지 않으며 할당량 조회는 별도로 제한됩니다. 끄기는 감시를 비활성화하고, 큰 기록에서는 60~300초가 CPU 부담을 가장 줄입니다.'
pt-BR: 'Tempo de espera após a última alteração JSONL local antes de atualizar (quiet debounce; cada novo evento reinicia a espera). Nenhuma API é chamada; as consultas de cota são limitadas separadamente. “Desligado” desativa o monitoramento, e 60–300 s reduz mais a CPU em históricos grandes.'
```

上面是翻译内容清单，不是可直接粘贴的对象字面量：实现者要替换 `SETTINGS_I18N` 中现有六个 `fileWatchSeconds.help` 字符串，并保留各语言已有的 `label`。

把 `src/extension.ts` 的 auto-refresh 改为固定服从配置。**保留** `lastActivityAt`、`isActive()` 和 JSONL watcher 中的 `this.lastActivityAt = Date.now()`；它们仍供 `maybeFetchUsageLimits()` 的 60/120 秒 quota TTL 使用，只删除 poll 对 activity 的依赖：

```ts
import { pollIntervalMs } from './refreshPolicy';

private startAutoRefresh(): void {
  if (this.refreshTimer) {
    clearTimeout(this.refreshTimer);
    this.refreshTimer = undefined;
  }
  const gen = ++this.refreshGen;
  const tick = (): void => {
    if (gen !== this.refreshGen) {
      return;
    }
    const intervalMs = pollIntervalMs(this.getConfiguration().refreshInterval);
    this.refreshTimer = setTimeout(() => {
      // Task 4 才增加显式 trigger 参数；此任务先保持现有签名可编译。
      this.refreshData().finally(() => {
        if (gen === this.refreshGen) {
          tick();
        }
      });
    }, intervalMs);
  };
  tick();
}
```

- [ ] **Step 4: 运行 GREEN 并确认旧的 8 秒路径已消失**

Run: `npm run compile && node --test out/test/refreshPolicy.test.js`

Expected: PASS，3 tests、0 failures。

Run: `rg -n "this\.isActive\(\) \? Math\.min|Math\.min\(base, 8000\)|active.*8.*poll" src/extension.ts`

Expected: 无输出，退出码 1。不要搜索裸 `8000`：quota 的冷启动重试仍合法使用 8 秒；`lastActivityAt` / `isActive()` 也必须继续存在。

Run: `rg -n "lastActivityAt|isActive\(\).*60000.*120000|const ttl = this\.isActive" src/extension.ts`

Expected: 可见 watcher activity 更新、`isActive()` 定义和 quota TTL 使用；poll timer 中不可见 `isActive()`。

- [ ] **Step 5: 提交 cadence 变更**

```bash
git add src/refreshPolicy.ts src/test/refreshPolicy.test.ts src/settings.ts src/i18n.ts src/types.ts src/extension.ts
git commit -m "fix: honor configured refresh interval"
```

### Task 2: 一次 manifest 扫描与有界首时间戳读取

**Files:**
- Create: `src/claudeUsageFiles.ts`
- Create: `src/test/claudeUsageFiles.test.ts`

**Interfaces:**
- Consumes: Claude data root 列表；每个 root 下读取 `projects/**/*.jsonl`。
- Produces: `UsageFileFingerprint`、`UsageManifest`、`ManifestDelta`、`isMissingPathError()`、`scanUsageManifest()`、`diffUsageManifests()`、`mapWithConcurrency()`、`readEarliestTimestamp()`、`sortUsageFilesByEarliestTimestamp()`。

- [ ] **Step 1: 写 manifest、短路读取和并发上限的失败测试**

```ts
// src/test/claudeUsageFiles.test.ts
import { after, test } from 'node:test';
import * as assert from 'node:assert/strict';
import { mkdtemp, mkdir, readdir, rename, rm, stat, utimes, writeFile } from 'node:fs/promises';
import * as os from 'node:os';
import * as path from 'node:path';
import {
  diffUsageManifests,
  isMissingPathError,
  mapWithConcurrency,
  readEarliestTimestamp,
  scanUsageManifest,
  sortUsageFilesByEarliestTimestamp,
} from '../claudeUsageFiles';

const roots: string[] = [];
after(async () => {
  await Promise.all(roots.map((root) => rm(root, { recursive: true, force: true })));
});

async function fixture(): Promise<{ root: string; file: string }> {
  const root = await mkdtemp(path.join(os.tmpdir(), 'ccu-files-'));
  roots.push(root);
  const project = path.join(root, 'projects', '-tmp-demo');
  await mkdir(project, { recursive: true });
  return { root, file: path.join(project, 'a.jsonl') };
}

test('manifest scanning tolerates only disappearance races', () => {
  assert.equal(isMissingPathError(Object.assign(new Error('gone'), { code: 'ENOENT' })), true);
  assert.equal(isMissingPathError(Object.assign(new Error('denied'), { code: 'EACCES' })), false);
  assert.equal(isMissingPathError(Object.assign(new Error('io'), { code: 'EIO' })), false);
  assert.equal(isMissingPathError(new Error('unknown')), false);
});

test('manifest scanning fails closed on permission errors but tolerates stat ENOENT', async () => {
  const { root, file } = await fixture();
  await writeFile(file, '{"timestamp":"2026-01-01T00:00:00.000Z"}\n');
  const readDirectory = (dir: string) => readdir(dir, { withFileTypes: true });
  await assert.rejects(
    () => scanUsageManifest([root], {
      readdir: readDirectory,
      stat: async () => { throw Object.assign(new Error('denied'), { code: 'EACCES' }); },
    }),
    (error: NodeJS.ErrnoException) => error.code === 'EACCES'
  );
  const missing = await scanUsageManifest([root], {
    readdir: readDirectory,
    stat: async () => { throw Object.assign(new Error('gone'), { code: 'ENOENT' }); },
  });
  assert.equal(missing.entries.size, 0);
});

test('manifest diff reports changed, reused, and removed without exposing them in counters', async () => {
  const { root, file } = await fixture();
  await writeFile(file, '{"timestamp":"2026-01-01T00:00:00.000Z"}\n');
  const first = await scanUsageManifest([root]);
  const cold = diffUsageManifests(null, first);
  assert.equal(cold.changed.length, 1);
  assert.equal(cold.reused.length, 0);

  const second = await scanUsageManifest([root]);
  assert.equal(diffUsageManifests(first, second).reused.length, 1);

  await writeFile(file, '{"timestamp":"2026-01-02T00:00:00.000Z"}\nlonger\n');
  const third = await scanUsageManifest([root]);
  assert.equal(diffUsageManifests(second, third).changed.length, 1);

  const replacement = `${file}.replacement`;
  await writeFile(replacement, '{"timestamp":"2026-01-03T00:00:00.000Z"}\nlonger\n');
  await rename(replacement, file);
  const fourth = await scanUsageManifest([root]);
  assert.equal(diffUsageManifests(third, fourth).changed.length, 1);

  await rm(file);
  const fifth = await scanUsageManifest([root]);
  assert.equal(diffUsageManifests(fourth, fifth).removed.length, 1);
});

test('inode detects an atomic replacement with unchanged size and mtime', async (t) => {
  const { root, file } = await fixture();
  const original = '{"timestamp":"2026-01-01T00:00:00.000Z"}\n';
  const replacementBody = '{"timestamp":"2026-01-02T00:00:00.000Z"}\n';
  assert.equal(Buffer.byteLength(original), Buffer.byteLength(replacementBody));
  await writeFile(file, original);
  const beforeManifest = await scanUsageManifest([root]);
  const before = await stat(file);

  const replacement = `${file}.replacement`;
  await writeFile(replacement, replacementBody);
  await utimes(replacement, before.atime, before.mtime);
  const prepared = await stat(replacement);
  assert.equal(prepared.size, before.size);
  if (prepared.mtimeMs !== before.mtimeMs) {
    t.skip('filesystem cannot preserve the exact mtime for this identity fixture');
    return;
  }
  await rename(replacement, file);
  const after = await stat(file);
  if (!(before.dev > 0 && before.ino > 0 && after.dev > 0 && after.ino > 0 &&
        (before.dev !== after.dev || before.ino !== after.ino))) {
    t.skip('filesystem does not expose a reliable changed file identity');
    return;
  }

  const afterManifest = await scanUsageManifest([root]);
  assert.equal(diffUsageManifests(beforeManifest, afterManifest).changed.length, 1);
});

test('earliest timestamp stops after the first valid JSONL record', async () => {
  const { file } = await fixture();
  const first = '{"timestamp":"2026-02-03T04:05:06.000Z"}\n';
  await writeFile(file, first + 'x'.repeat(512 * 1024));
  const result = await readEarliestTimestamp(file);
  assert.equal(result.timestampMs, Date.parse('2026-02-03T04:05:06.000Z'));
  assert.ok(result.bytesRead < 64 * 1024, `read ${result.bytesRead} bytes`);
});

test('a valid first JSONL record larger than 1 MiB keeps its timestamp', async () => {
  const { file } = await fixture();
  const body = JSON.stringify({
    timestamp: '2026-02-03T04:05:06.000Z',
    message: { content: '中🙂'.repeat(180_000) },
  }) + '\n';
  await writeFile(file, body + '{"timestamp":"2026-03-01T00:00:00.000Z"}\n');
  const result = await readEarliestTimestamp(file);
  assert.equal(result.timestampMs, Date.parse('2026-02-03T04:05:06.000Z'));
  assert.ok(result.bytesRead > 1024 * 1024);
});

test('timestamp probing is bounded when no valid timestamp exists', async () => {
  const { file } = await fixture();
  const content = 'not-json\n'.repeat(150_000);
  await writeFile(file, content);
  await assert.rejects(
    () => readEarliestTimestamp(file),
    { name: 'TimestampProbeLimitError' }
  );
});

test('mapWithConcurrency never starts more than eight readers', async () => {
  let active = 0;
  let peak = 0;
  const result = await mapWithConcurrency(
    Array.from({ length: 32 }, (_, i) => i),
    8,
    async (value) => {
      active += 1;
      peak = Math.max(peak, active);
      await new Promise<void>((resolve) => setImmediate(resolve));
      active -= 1;
      return value * 2;
    }
  );
  assert.equal(peak, 8);
  assert.deepEqual(result, Array.from({ length: 32 }, (_, i) => i * 2));
});

test('equal or invalid timestamps preserve v2.2.0 discovery order', async () => {
  const { root } = await fixture();
  const project = path.join(root, 'projects', '-tmp-demo');
  const z = path.join(project, 'z.jsonl');
  const a = path.join(project, 'a.jsonl');
  const invalid = path.join(project, 'invalid.jsonl');
  const invalid2 = path.join(project, 'invalid-2.jsonl');
  await writeFile(z, '{"timestamp":"2026-01-01T00:00:00.000Z"}\n');
  await writeFile(a, '{"timestamp":"2026-01-01T00:00:00.000Z"}\n');
  await writeFile(invalid, '{"timestamp":"not-a-date"}\n');
  await writeFile(invalid2, 'not-json\n');

  const make = async (file: string, discoveryIndex: number) => {
    const fileStat = await stat(file);
    return {
      path: file,
      size: fileStat.size,
      mtimeMs: fileStat.mtimeMs,
      discoveryIndex,
      dev: fileStat.dev > 0 && fileStat.ino > 0 ? fileStat.dev : undefined,
      ino: fileStat.dev > 0 && fileStat.ino > 0 ? fileStat.ino : undefined,
    };
  };

  const equal = await sortUsageFilesByEarliestTimestamp([
    await make(z, 0),
    await make(a, 1),
  ]);
  assert.deepEqual(equal.files, [z, a]); // do not add a path/name tie-breaker

  const invalidPair = await sortUsageFilesByEarliestTimestamp([
    await make(invalid, 0),
    await make(invalid2, 1),
  ]);
  assert.deepEqual(invalidPair.files, [invalid, invalid2]);
});
```

- [ ] **Step 2: 运行 RED，确认文件层接口尚不存在**

Run: `npm run compile`

Expected: FAIL，包含 `TS2307: Cannot find module '../claudeUsageFiles'`。

- [ ] **Step 3: 实现 manifest、fingerprint 比较与流式排序**

```ts
// src/claudeUsageFiles.ts
import * as fs from 'node:fs';
import * as path from 'node:path';
import { StringDecoder } from 'node:string_decoder';

export interface UsageFileFingerprint {
  path: string;
  size: number;
  mtimeMs: number;
  /** Original v2.2.0 recursive discovery position; tie ordering depends on it. */
  discoveryIndex: number;
  dev?: number;
  ino?: number;
}

export interface UsageManifest {
  entries: ReadonlyMap<string, UsageFileFingerprint>;
  scannedAtMs: number;
  scanDurationMs: number;
}

export interface ManifestDelta {
  changed: UsageFileFingerprint[];
  reused: UsageFileFingerprint[];
  removed: UsageFileFingerprint[];
}

export function isMissingPathError(error: unknown): boolean {
  return (error as NodeJS.ErrnoException | undefined)?.code === 'ENOENT';
}

export interface UsageFileSystem {
  readdir(dir: string): Promise<fs.Dirent[]>;
  stat(filePath: string): Promise<fs.Stats>;
}

const DEFAULT_USAGE_FILE_SYSTEM: UsageFileSystem = {
  readdir: (dir) => fs.promises.readdir(dir, { withFileTypes: true }),
  stat: (filePath) => fs.promises.stat(filePath),
};

async function walkJsonl(
  dir: string,
  output: string[],
  io: UsageFileSystem
): Promise<void> {
  let entries: fs.Dirent[];
  try {
    entries = await io.readdir(dir);
  } catch (error) {
    if (isMissingPathError(error)) return;
    throw error;
  }
  // Do not sort readdir results. v2.2.0 used native recursive discovery order,
  // and stable Array.sort preserved that order for equal/invalid timestamps.
  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      await walkJsonl(fullPath, output, io);
    } else if (entry.isFile() && entry.name.endsWith('.jsonl')) {
      output.push(fullPath);
    }
  }
}

export async function scanUsageManifest(
  dataRoots: readonly string[],
  io: UsageFileSystem = DEFAULT_USAGE_FILE_SYSTEM
): Promise<UsageManifest> {
  const started = performance.now();
  const paths: string[] = [];
  for (const root of dataRoots) {
    await walkJsonl(path.join(root, 'projects'), paths, io);
  }
  const entries = new Map<string, UsageFileFingerprint>();
  for (const [discoveryIndex, filePath] of paths.entries()) {
    try {
      const stat = await io.stat(filePath);
      const hasStableIdentity = stat.dev > 0 && stat.ino > 0;
      entries.set(filePath, {
        path: filePath,
        size: stat.size,
        mtimeMs: stat.mtimeMs,
        discoveryIndex,
        dev: hasStableIdentity ? stat.dev : undefined,
        ino: hasStableIdentity ? stat.ino : undefined,
      });
    } catch (error) {
      // A file may disappear between readdir and stat. Permission and I/O
      // failures are not an empty corpus: abort this reconciliation instead.
      if (!isMissingPathError(error)) throw error;
    }
  }
  return { entries, scannedAtMs: Date.now(), scanDurationMs: performance.now() - started };
}

export function sameFingerprint(a: UsageFileFingerprint, b: UsageFileFingerprint): boolean {
  const identityComparable = (a.dev ?? 0) > 0 && (a.ino ?? 0) > 0 &&
    (b.dev ?? 0) > 0 && (b.ino ?? 0) > 0;
  return a.path === b.path && a.size === b.size && a.mtimeMs === b.mtimeMs &&
    (!identityComparable || (a.dev === b.dev && a.ino === b.ino));
}

export function diffUsageManifests(
  previous: UsageManifest | null,
  current: UsageManifest
): ManifestDelta {
  if (!previous) {
    return { changed: [...current.entries.values()], reused: [], removed: [] };
  }
  const changed: UsageFileFingerprint[] = [];
  const reused: UsageFileFingerprint[] = [];
  for (const entry of current.entries.values()) {
    const old = previous.entries.get(entry.path);
    (old && sameFingerprint(old, entry) ? reused : changed).push(entry);
  }
  const removed = [...previous.entries.values()].filter((entry) => !current.entries.has(entry.path));
  return { changed, reused, removed };
}

export async function mapWithConcurrency<T, R>(
  values: readonly T[],
  limit: number,
  worker: (value: T, index: number) => Promise<R>
): Promise<R[]> {
  if (!Number.isInteger(limit) || limit < 1) {
    throw new RangeError('limit must be a positive integer');
  }
  const output = new Array<R>(values.length);
  let next = 0;
  const run = async (): Promise<void> => {
    while (next < values.length) {
      const index = next++;
      output[index] = await worker(values[index], index);
    }
  };
  await Promise.all(Array.from({ length: Math.min(limit, values.length) }, run));
  return output;
}

function timestampFromLine(line: string): number | null {
  if (line.trim() === '') return null;
  try {
    const value = JSON.parse(line) as { timestamp?: unknown };
    if (typeof value.timestamp !== 'string') return null;
    const timestampMs = Date.parse(value.timestamp);
    return Number.isNaN(timestampMs) ? null : timestampMs;
  } catch {
    return null;
  }
}

export async function readEarliestTimestamp(
  filePath: string
): Promise<{ timestampMs: number; bytesRead: number }> {
  // Never cap an in-progress JSONL record: legitimate snapshot/tool/prompt
  // records may exceed 1 MiB. Only completed invalid records consume the
  // fail-closed invalid-prefix budget.
  const MAX_COMPLETED_INVALID_PREFIX_BYTES = 1024 * 1024;
  const stream = fs.createReadStream(filePath, {
    highWaterMark: 4096,
    start: 0,
  });
  const decoder = new StringDecoder('utf8');
  let lineParts: string[] = [];
  let bytesRead = 0;
  let completedInvalidBytes = 0;
  try {
    for await (const chunk of stream) {
      const buffer = chunk as Buffer;
      bytesRead += buffer.byteLength;
      const pieces = decoder.write(buffer).split('\n');
      lineParts.push(pieces.shift()!);
      while (pieces.length > 0) {
        const line = lineParts.join('').replace(/\r$/, '');
        lineParts = [pieces.shift()!];
        const timestampMs = timestampFromLine(line);
        if (timestampMs !== null) return { timestampMs, bytesRead };
        completedInvalidBytes += Buffer.byteLength(`${line}\n`, 'utf8');
        if (completedInvalidBytes > MAX_COMPLETED_INVALID_PREFIX_BYTES) {
          const error = new Error('invalid timestamp prefix limit exceeded');
          error.name = 'TimestampProbeLimitError';
          throw error;
        }
      }
    }
    const finalDecoded = decoder.end();
    if (finalDecoded !== '') lineParts.push(finalDecoded);
    const pending = lineParts.join('');
    const finalTimestamp = timestampFromLine(pending);
    if (finalTimestamp !== null) return { timestampMs: finalTimestamp, bytesRead };
    completedInvalidBytes += Buffer.byteLength(pending, 'utf8');
    if (completedInvalidBytes > MAX_COMPLETED_INVALID_PREFIX_BYTES) {
      const error = new Error('invalid timestamp prefix limit exceeded');
      error.name = 'TimestampProbeLimitError';
      throw error;
    }
    return { timestampMs: 0, bytesRead };
  } catch (error) {
    stream.destroy();
    throw error;
  }
}

export async function sortUsageFilesByEarliestTimestamp(
  entries: readonly UsageFileFingerprint[]
): Promise<{ files: string[]; bytesRead: number }> {
  const stamped = await mapWithConcurrency(entries, 8, async (entry) => ({
    file: entry.path,
    discoveryIndex: entry.discoveryIndex,
    ...(await readEarliestTimestamp(entry.path)),
  }));
  stamped.sort((a, b) =>
    a.timestampMs - b.timestampMs || a.discoveryIndex - b.discoveryIndex
  );
  return {
    files: stamped.map((item) => item.file),
    bytesRead: stamped.reduce((sum, item) => sum + item.bytesRead, 0),
  };
}
```

`lineParts` 只在遇到 newline 时 join，避免对超大合法首 record 每 4 KiB 重复复制整条字符串造成 O(n²) CPU；返回/失败后不得把这些 parts 存入 cache。

- [ ] **Step 4: 运行 GREEN 并重复并发测试以排除偶发超限**

Run: `npm run compile && node --test out/test/claudeUsageFiles.test.js && node --test out/test/claudeUsageFiles.test.js`

Expected: 两次均 PASS，9 tests、0 failures（不提供可靠 inode 的文件系统可 skip identity case）；`ENOENT` 是唯一可忽略 scanner errno，`EACCES` test 必须 reject；同时间戳和非法时间戳测试不得按路径名重排；超过 1 MiB 的完整合法首行仍取得原 timestamp。只有已经完整读完且无合法 timestamp 的累计 invalid records 超过 1 MiB 才 reject 并保持旧 snapshot；绝不能截断当前 record 或降级成错误的 `timestampMs=0` 后提交。

- [ ] **Step 5: 提交文件发现与有界排序模块**

```bash
git add src/claudeUsageFiles.ts src/test/claudeUsageFiles.test.ts
git commit -m "perf: bound JSONL timestamp scanning"
```

### Task 3: loader 复用 manifest 并返回匿名 I/O 指标

**Files:**
- Create: `src/refreshDiagnostics.ts`
- Create: `src/test/refreshDiagnostics.test.ts`
- Modify: `src/dataLoader.ts:1-69,522-876,975-1014,2843-2872`
- Modify: `src/test/dataLoader.test.ts`

**Interfaces:**
- Consumes: Task 2 的 `UsageManifest`、`scanUsageManifest()`、`sortUsageFilesByEarliestTimestamp()`。
- Produces: `LoadUsageDiagnostics`、扩展后的 `loadUsageRecords(..., options.manifest)` 返回值、`formatRefreshDiagnostic(metrics): string`。

- [ ] **Step 1: 写 loader 只扫描一次及诊断不含敏感字段的失败测试**

```ts
// append to src/test/dataLoader.test.ts
import { after } from 'node:test';
import { mkdtemp, mkdir, rm, writeFile } from 'node:fs/promises';
import * as os from 'node:os';
import * as path from 'node:path';
import { scanUsageManifest } from '../claudeUsageFiles';

const tempRoots: string[] = [];
after(async () => Promise.all(tempRoots.map((root) => rm(root, { recursive: true, force: true }))));

test('loadUsageRecords uses the injected manifest instead of rescanning dataDirectory', async () => {
  const manifestRoot = await mkdtemp(path.join(os.tmpdir(), 'ccu-loader-manifest-'));
  const emptyArgumentRoot = await mkdtemp(path.join(os.tmpdir(), 'ccu-loader-empty-'));
  tempRoots.push(manifestRoot, emptyArgumentRoot);
  const project = path.join(manifestRoot, 'projects', '-tmp-project');
  await mkdir(project, { recursive: true });
  await mkdir(path.join(emptyArgumentRoot, 'projects'), { recursive: true });
  const line = JSON.stringify({
    timestamp: '2026-07-17T00:00:00.000Z',
    requestId: 'request-secret',
    message: {
      id: 'message-secret',
      model: 'claude-sonnet-4-6',
      usage: { input_tokens: 10, output_tokens: 2 },
    },
  }) + '\n';
  await writeFile(path.join(project, 'session-secret.jsonl'), line);
  const manifest = await scanUsageManifest([manifestRoot]);

  // Passing an empty, different root is deliberate: if the loader ignores the
  // injected manifest and performs its own scan, this assertion returns zero.
  const loaded = await ClaudeDataLoader.loadUsageRecords(emptyArgumentRoot, {
    analyzeContent: false,
    manifest,
  });
  assert.equal(loaded.records.length, 1);
  assert.equal(loaded.diagnostics.filesDiscovered, 1);
  assert.equal(loaded.diagnostics.bytesRead >= Buffer.byteLength(line), true);
  assert.equal(loaded.diagnostics.linesParsed, 1);
  assert.equal(loaded.diagnostics.filesFailed, 0);
});

test('a file that disappears after manifest scan marks the load incomplete', async () => {
  const manifestRoot = await mkdtemp(path.join(os.tmpdir(), 'ccu-loader-race-'));
  tempRoots.push(manifestRoot);
  const project = path.join(manifestRoot, 'projects', '-tmp-project');
  await mkdir(project, { recursive: true });
  const file = path.join(project, 'vanishing.jsonl');
  await writeFile(file, '{"timestamp":"2026-07-17T00:00:00.000Z"}\n');
  const manifest = await scanUsageManifest([manifestRoot]);
  await rm(file);
  const loaded = await ClaudeDataLoader.loadUsageRecords(manifestRoot, {
    analyzeContent: false,
    manifest,
  });
  assert.equal(loaded.diagnostics.filesFailed, 1);
});
```

```ts
// src/test/refreshDiagnostics.test.ts
import { test } from 'node:test';
import * as assert from 'node:assert/strict';
import { formatRefreshDiagnostic } from '../refreshDiagnostics';

test('refresh diagnostics contain only stage names and anonymous numeric counters', () => {
  const line = formatRefreshDiagnostic({
    trigger: 'watch',
    filesDiscovered: 389,
    filesChanged: 1,
    filesReused: 388,
    filesRemoved: 0,
    filesFailed: 0,
    bytesRead: 4096,
    linesParsed: 17,
    watcherEvents: 42,
    coalescedTriggers: 3,
    manifestMs: 12.34,
    readParseMs: 45.67,
    aggregateRenderMs: 8.9,
    totalMs: 67.01,
  });
  assert.equal(
    line,
    'refresh: trigger=watch files(discovered=389 changed=1 reused=388 removed=0 failed=0) ' +
      'io(bytes=4096 lines=17) events(watcher=42 coalesced=3) ' +
      'ms(manifest=12.3 read-parse=45.7 aggregate-render=8.9 total=67.0)'
  );
  assert.equal(/[/\\]|secret|session|prompt|credential|\.jsonl/i.test(line), false);
});
```

- [ ] **Step 2: 运行 RED，确认 loader 结果尚无 diagnostics**

Run: `npm run compile`

Expected: FAIL，至少包含 `Property 'diagnostics' does not exist` 和缺少 `refreshDiagnostics` 模块。

- [ ] **Step 3: 加入匿名类型、复用 manifest 并移除旧全文件时间戳实现**

```ts
// src/refreshDiagnostics.ts
import { RefreshTrigger } from './refreshPolicy';

export interface LoadUsageDiagnostics {
  filesDiscovered: number;
  filesFailed: number;
  bytesRead: number;
  linesParsed: number;
  readParseMs: number;
}

export interface RefreshDiagnostic extends LoadUsageDiagnostics {
  trigger: RefreshTrigger;
  filesChanged: number;
  filesReused: number;
  filesRemoved: number;
  watcherEvents: number;
  coalescedTriggers: number;
  manifestMs: number;
  aggregateRenderMs: number;
  totalMs: number;
}

const ms = (value: number): string => value.toFixed(1);

export function formatRefreshDiagnostic(value: RefreshDiagnostic): string {
  return `refresh: trigger=${value.trigger} ` +
    `files(discovered=${value.filesDiscovered} changed=${value.filesChanged} ` +
    `reused=${value.filesReused} removed=${value.filesRemoved} failed=${value.filesFailed}) ` +
    `io(bytes=${value.bytesRead} lines=${value.linesParsed}) ` +
    `events(watcher=${value.watcherEvents} coalesced=${value.coalescedTriggers}) ` +
    `ms(manifest=${ms(value.manifestMs)} read-parse=${ms(value.readParseMs)} ` +
    `aggregate-render=${ms(value.aggregateRenderMs)} total=${ms(value.totalMs)})`;
}
```

在 `src/dataLoader.ts` 导入 Task 2/3 接口，删除文件顶部的本地 `findJsonlFiles()`，并把 loader 签名改为：

```ts
import {
  scanUsageManifest,
  sortUsageFilesByEarliestTimestamp,
  UsageManifest,
} from './claudeUsageFiles';
import { LoadUsageDiagnostics } from './refreshDiagnostics';

static async loadUsageRecords(
  dataDirectory?: string,
  options?: {
    analyzeContent?: boolean;
    windowDays?: number;
    log?: (line: string) => void;
    manifest?: UsageManifest;
  }
): Promise<{
  records: ClaudeUsageRecord[];
  contentAnalysis: ContentAnalysis | null;
  diagnostics: LoadUsageDiagnostics;
}> {
  const readParseStarted = performance.now();
  let bytesRead = 0;
  let linesParsed = 0;
  let filesFailed = 0;
  const emptyDiagnostics = (): LoadUsageDiagnostics => ({
    filesDiscovered: 0,
    filesFailed,
    bytesRead,
    linesParsed,
    readParseMs: performance.now() - readParseStarted,
  });
  try {
    const claudePaths = dataDirectory ? [dataDirectory] : this.getClaudePaths();
    const manifest = options?.manifest ?? await scanUsageManifest(claudePaths);
    const sorted = await sortUsageFilesByEarliestTimestamp([...manifest.entries.values()]);
    bytesRead += sorted.bytesRead;
    const sortedFiles = sorted.files;

    // 保留下方现有逐行解析、去重、title、analysis 与 attribution 逻辑；
    // 读取每个文件后立即累计实际 UTF-8 字节数：
    for (const file of sortedFiles) {
      try {
        const content = await readFile(file, 'utf-8');
        bytesRead += Buffer.byteLength(content, 'utf8');
        const lines = content.trim().split('\n').filter((line) => line.trim() !== '');
        for (const line of lines) {
          linesParsed += 1;
          // 此处继续执行当前 JSON.parse/validate/dedup/analysis 分支，顺序不变。
        }
      } catch {
        // Preserve the old continue-on-one-bad-file behavior, but make the
        // incomplete load explicit so extension never commits its manifest.
        filesFailed += 1;
      }
    }

    // 在现有 finalizeAnalysis/calibration 完成后返回：
    return {
      records,
      contentAnalysis,
      diagnostics: {
        filesDiscovered: manifest.entries.size,
        filesFailed,
        bytesRead,
        linesParsed,
        readParseMs: performance.now() - readParseStarted,
      },
    };
  } catch (error) {
    filesFailed = Math.max(filesFailed, 1);
    console.error('Error loading usage records:', error);
    return { records: [], contentAnalysis: null, diagnostics: emptyDiagnostics() };
  }
}
```

机械删除 `src/dataLoader.ts` 的旧 `getEarliestTimestamp()` / `sortFilesByTimestamp()`，并让兼容 API 复用 manifest：

```ts
static async getLatestModifiedTime(dataDirectory?: string): Promise<number> {
  try {
    const roots = dataDirectory ? [dataDirectory] : this.getClaudePaths();
    const manifest = await scanUsageManifest(roots);
    let latest = 0;
    for (const entry of manifest.entries.values()) {
      latest = Math.max(latest, entry.mtimeMs);
    }
    return latest;
  } catch {
    return 0;
  }
}
```

实现时不得复制第二套逐行 parser；应把当前 `src/dataLoader.ts:545-870` 的主体原地保留，仅替换它的文件列表来源、计数点和返回结构。现有 `stats`（`parseErrors`、`rejected`、dedup、kept、user prompts、model summaries）和它通过 `options.log` 输出的诊断必须原样保留；`LoadUsageDiagnostics` 是额外的匿名性能计数，不能取代现有格式漂移诊断。

- [ ] **Step 4: 运行 focused GREEN 与完整 loader 回归**

Run: `npm run compile && node --test out/test/claudeUsageFiles.test.js out/test/refreshDiagnostics.test.js out/test/dataLoader.test.js`

Expected: PASS，所有列出的测试 0 failures。

Run: `rg -n "getEarliestTimestamp|sortFilesByTimestamp|Promise\.all\(\s*files\.map" src/dataLoader.ts`

Expected: 无输出，退出码 1。

- [ ] **Step 5: 提交单扫描 loader 与匿名诊断类型**

```bash
git add src/dataLoader.ts src/refreshDiagnostics.ts src/test/dataLoader.test.ts src/test/refreshDiagnostics.test.ts
git commit -m "perf: reuse JSONL manifest during loads"
```

### Task 4: 把 trigger、quiet watcher 和阶段诊断接入 extension

**Files:**
- Modify: `src/extension.ts:39-97,120-127,181-184,670-954,1040-1204`
- Modify: `src/test/refreshPolicy.test.ts`

**Interfaces:**
- Consumes: `RefreshTrigger`、`mergeRefreshTrigger()`、`scanUsageManifest()`、`diffUsageManifests()`、`formatRefreshDiagnostic()`、loader 的 `diagnostics`。
- Produces: `refreshData(forceReload?: boolean, trigger?: RefreshTrigger)`、`RefreshSingleFlight`；watcher burst 只保留一个 quiet timer；每个实际 refresh 输出一条匿名阶段日志。

- [ ] **Step 1: 先补上 watcher burst 与 pending trigger 的 RED 契约**

在 `src/refreshPolicy.ts` 增加导出声明前，先把以下测试追加到 `src/test/refreshPolicy.test.ts`：

```ts
import {
  QuietDebounce,
  RefreshSingleFlight,
  shouldCommitUsageLoad,
  shouldReloadUsage,
} from '../refreshPolicy';

test('quiet debounce collapses a write burst into one callback', () => {
  const scheduled: Array<() => void> = [];
  const delays: number[] = [];
  const cleared: number[] = [];
  const debounce = new QuietDebounce(
    (callback, ms) => {
      scheduled.push(callback);
      delays.push(ms);
      return scheduled.length as unknown as NodeJS.Timeout;
    },
    (handle) => { cleared.push(handle as unknown as number); }
  );
  let fired = 0;
  debounce.push(2_000, () => { fired += 1; });
  debounce.push(300_000, () => { fired += 1; });
  debounce.push(60_000, () => { fired += 1; });
  assert.deepEqual(delays, [2_000, 300_000, 60_000]);
  assert.deepEqual(cleared, [1, 2]);
  scheduled[2]();
  assert.equal(fired, 1);
});

test('single-flight collapses a burst to one strongest follow-up', () => {
  const gate = new RefreshSingleFlight();
  assert.deepEqual(gate.request(false, 'poll'), { forceReload: false, trigger: 'poll' });
  for (let i = 0; i < 50; i += 1) {
    assert.equal(gate.request(false, 'watch'), null);
  }
  assert.equal(gate.request(true, 'manual'), null);
  assert.deepEqual(gate.complete(), { forceReload: true, trigger: 'manual' });

  // The returned follow-up owns the gate; a trigger arriving before it runs is
  // queued for one later pass instead of starting in parallel.
  assert.equal(gate.request(false, 'focus'), null);
  assert.deepEqual(gate.complete(), { forceReload: false, trigger: 'focus' });
  assert.equal(gate.complete(), null);
  assert.deepEqual(gate.request(false, 'poll'), { forceReload: false, trigger: 'poll' });
});

test('a successfully loaded empty corpus reuses its non-null manifest', () => {
  assert.equal(shouldReloadUsage({
    forceReload: false,
    directoryChanged: false,
    hasLoadedManifest: true,
    changedFiles: 0,
    removedFiles: 0,
  }), false);
  assert.equal(shouldReloadUsage({
    forceReload: false,
    directoryChanged: false,
    hasLoadedManifest: false,
    changedFiles: 0,
    removedFiles: 0,
  }), true);
});

test('an incomplete file load cannot commit the new manifest', () => {
  assert.equal(shouldCommitUsageLoad(0), true);
  assert.equal(shouldCommitUsageLoad(1), false);
});
```

- [ ] **Step 2: 运行 RED，确认 quiet-debounce helper 尚未定义**

Run: `npm run compile`

Expected: FAIL，包含缺少 `QuietDebounce` / `RefreshSingleFlight` 导出的错误。

- [ ] **Step 3: 实现 debounce helper 并接入 extension 的单次 manifest/诊断路径**

```ts
// append to src/refreshPolicy.ts
export class QuietDebounce {
  private handle: NodeJS.Timeout | undefined;

  constructor(
    private readonly schedule: (callback: () => void, ms: number) => NodeJS.Timeout = setTimeout,
    private readonly cancel: (handle: NodeJS.Timeout) => void = clearTimeout
  ) {}

  push(ms: number, callback: () => void): void {
    if (this.handle) this.cancel(this.handle);
    this.handle = this.schedule(() => {
      this.handle = undefined;
      callback();
    }, ms);
  }

  clear(): void {
    if (this.handle) this.cancel(this.handle);
    this.handle = undefined;
  }
}

export interface RefreshRequest {
  forceReload: boolean;
  trigger: RefreshTrigger;
}

export function shouldReloadUsage(value: {
  forceReload: boolean;
  directoryChanged: boolean;
  hasLoadedManifest: boolean;
  changedFiles: number;
  removedFiles: number;
}): boolean {
  return value.forceReload || value.directoryChanged || !value.hasLoadedManifest ||
    value.changedFiles > 0 || value.removedFiles > 0;
}

export function shouldCommitUsageLoad(filesFailed: number): boolean {
  return Number.isInteger(filesFailed) && filesFailed === 0;
}

export class RefreshSingleFlight {
  private active = false;
  private pendingForceReload = false;
  private pendingTrigger: RefreshTrigger | null = null;

  request(forceReload: boolean, trigger: RefreshTrigger): RefreshRequest | null {
    if (!this.active) {
      this.active = true;
      return { forceReload, trigger };
    }
    this.pendingForceReload ||= forceReload;
    this.pendingTrigger = mergeRefreshTrigger(this.pendingTrigger, trigger);
    return null;
  }

  complete(): RefreshRequest | null {
    if (this.pendingTrigger === null) {
      this.active = false;
      return null;
    }
    const next = {
      forceReload: this.pendingForceReload,
      trigger: this.pendingTrigger,
    };
    this.pendingForceReload = false;
    this.pendingTrigger = null;
    // Keep active=true: caller runs `next` directly without calling request().
    return next;
  }
}
```

在 `src/extension.ts` 加入并初始化这些字段；从 `cache` 中的 manifest 只保存匿名比较所需对象，不持久化：

```ts
import {
  diffUsageManifests,
  scanUsageManifest,
  UsageManifest,
} from './claudeUsageFiles';
import {
  mergeRefreshTrigger,
  QuietDebounce,
  RefreshRequest,
  RefreshSingleFlight,
  RefreshTrigger,
  shouldCommitUsageLoad,
  shouldReloadUsage,
} from './refreshPolicy';
import { formatRefreshDiagnostic } from './refreshDiagnostics';

private readonly watchDebounce = new QuietDebounce();
private readonly refreshGate = new RefreshSingleFlight();
private watcherEventsSinceRefresh = 0;
private coalescedTriggersSinceRefresh = 0;

// cache object gains this field and initial value:
manifest: UsageManifest | null;
// ...
manifest: null,
```

watch callback 必须只计数、quiet debounce、带上 trigger，不能改变 poll cadence：

```ts
this.fileWatcher = fs.watch(projectsDir, { recursive: true }, (_event, filename) => {
  if (!filename || !String(filename).endsWith('.jsonl')) return;
  // Activity remains authoritative for quota TTL only; poll cadence ignores it.
  this.lastActivityAt = Date.now();
  this.watcherEventsSinceRefresh += 1;
  // Read current settings for every event. Never close over the config captured
  // when the watcher was first attached: changing 2s -> 300s must take effect.
  const delaySeconds = this.getConfiguration().fileWatchSeconds;
  if (!(delaySeconds > 0)) {
    this.stopFileWatching();
    return;
  }
  this.watchDebounce.push(delaySeconds * 1000, () => {
    void this.refreshData(false, 'watch');
  });
});
```

`stopFileWatching()` 调用 `this.watchDebounce.clear()`，并删除旧 `watchDebounceTimer` 字段。`onConfigurationChanged()` 无条件先 `stopFileWatching()`，再刷新并重新创建 watcher；不能只在 raw `config.dataDirectory` 与已经 resolve 的 `cache.dataDirectory` 相等/不等时决定重启。动态读取 delay 与强制重启共同保证 2 秒切换到 300 秒或 Off 会立即生效。

所有现有 `refreshData` call site 必须按下表逐一转换；`forceReload=true` 表示重读并重新聚合，低频的 pricing/workspace/settings 允许为正确性付出一次全读，credentials 只失效 quota、不得无条件重读 JSONL：

| Call site | 精确调用 | force 语义 |
|---|---|---|
| activation / constructor (`~126`) | `this.refreshData(false, 'startup').then(() => this.startFileWatching())` | 冷 cache 由 `manifest === null` 自动触发读取 |
| manual command (`~183`) | `void this.refreshData(true, 'manual')` | 用户明确要求强制重读，且始终更新 webview |
| pricing refresh (`~223`) | `void this.refreshData(true, 'pricing')` | 新价格必须重新计算；删除 `cache.lastUpdate = new Date(0)` hack |
| workspace folders (`~696`) | `void this.refreshData(true, 'workspace')` | workspace filter/context 与 breakdown 必须重算 |
| settings/config (`~781`) | `this.stopFileWatching(); void this.refreshData(true, 'settings').then(() => this.startFileWatching())` | 包括 content analysis、grouping、data directory 与 watcher delay；全部重新聚合 |
| JSONL watcher (`~820`) | `void this.refreshData(false, 'watch')` | manifest 有变化才重读 |
| credentials watcher (`~875`) | `void this.refreshData(false, 'credentials')` | 只让 quota TTL 失效；JSONL 不变时走短路 |
| window focus (`~917`) | `void this.refreshData(false, 'focus')` | catch-up scan，不强制全读 |
| poll timer (`~946`) | `this.refreshData(false, 'poll').finally(...)` | 始终按配置 cadence，manifest 不变时短路 |

`onConfigurationChanged()` 不再拿 raw `config.dataDirectory` 与 resolved cache path 比较，也不以 `records=[]` 作为强制信号；resolved directory 是否变化在 `refreshData()` 找到目录后判断。

把 `refreshData` 拆成入队 wrapper 与只由 gate 调用的实际执行函数：

```ts
private async refreshData(
  forceReload: boolean = false,
  trigger: RefreshTrigger = 'poll'
): Promise<void> {
  const request = this.refreshGate.request(forceReload, trigger);
  if (request === null) {
    this.coalescedTriggersSinceRefresh += 1;
    return;
  }
  await this.runRefresh(request);
}
```

把实际 manifest/loader/返回路径改成以下确定流程；现有 quota/UI 聚合代码留在标出的阶段中：

```ts
private async runRefresh(request: RefreshRequest): Promise<void> {
  const totalStarted = performance.now();
  const watcherEvents = this.watcherEventsSinceRefresh;
  const coalescedTriggers = this.coalescedTriggersSinceRefresh;
  this.watcherEventsSinceRefresh = 0;
  this.coalescedTriggersSinceRefresh = 0;
  let aggregateRenderMs = 0;
  try {
    const config = this.getConfiguration();
    const updateWebview = request.trigger === 'manual' || config.dashboardAutoRefresh;
    // 保留现有 asynchronous quota fetch 和 dataDirectory 查找。
    const dataDirectory = await ClaudeDataLoader.findClaudeDataDirectory(
      config.dataDirectory || undefined
    );
    if (!dataDirectory) {
      // 保留现有无数据 UI 分支。
      return;
    }

    const manifestStarted = performance.now();
    const manifest = await scanUsageManifest([dataDirectory]);
    const delta = diffUsageManifests(this.cache.manifest, manifest);
    const manifestMs = performance.now() - manifestStarted;
    const dirChanged = this.cache.dataDirectory !== dataDirectory;
    const needFullRefresh = shouldReloadUsage({
      forceReload: request.forceReload,
      directoryChanged: dirChanged,
      hasLoadedManifest: this.cache.manifest !== null,
      changedFiles: delta.changed.length,
      removedFiles: delta.removed.length,
    });

    if (!needFullRefresh) {
      // 保留现有 cached context 更新。
      // 先保存成功 reconciliation，再输出诊断，再返回；合法空 corpus
      // 也会在第二次刷新命中此路径，不能因 records.length===0 反复全读。
      this.cache.manifest = manifest;
      this.cache.dataDirectory = dataDirectory;
      this.outputChannel.appendLine(formatRefreshDiagnostic({
        trigger: request.trigger,
        filesDiscovered: manifest.entries.size,
        filesChanged: 0,
        filesReused: delta.reused.length,
        filesRemoved: 0,
        filesFailed: 0,
        bytesRead: 0,
        linesParsed: 0,
        watcherEvents,
        coalescedTriggers,
        manifestMs,
        readParseMs: 0,
        aggregateRenderMs: 0,
        totalMs: performance.now() - totalStarted,
      }));
      return;
    }

    const loaded = await ClaudeDataLoader.loadUsageRecords(dataDirectory, {
      analyzeContent: config.enableContentAnalysis,
      windowDays: config.advicePromptWindowDays,
      manifest,
      log: (line) => this.outputChannel.appendLine(
        `[${new Date().toLocaleTimeString(undefined, { hour12: false })}] ${line}`
      ),
    });
    if (!shouldCommitUsageLoad(loaded.diagnostics.filesFailed)) {
      // A partial corpus must never become the new cache baseline. Keep the
      // previous records + manifest so the next trigger retries the same file.
      this.outputChannel.appendLine(formatRefreshDiagnostic({
        trigger: request.trigger,
        filesDiscovered: manifest.entries.size,
        filesChanged: delta.changed.length,
        filesReused: delta.reused.length,
        filesRemoved: delta.removed.length,
        filesFailed: loaded.diagnostics.filesFailed,
        bytesRead: loaded.diagnostics.bytesRead,
        linesParsed: loaded.diagnostics.linesParsed,
        watcherEvents,
        coalescedTriggers,
        manifestMs,
        readParseMs: loaded.diagnostics.readParseMs,
        aggregateRenderMs: 0,
        totalMs: performance.now() - totalStarted,
      }));
      return;
    }
    const aggregateStarted = performance.now();
    // 原有 records/contentAnalysis cache 赋值和所有聚合/UI 更新放在这里。
    // 冷启动 loading 判断改为 previous manifest===null，不能用 records.length。
    // records===[] 是合法成功结果：更新 no-data UI，但不要提前 return。
    aggregateRenderMs = performance.now() - aggregateStarted;
    this.cache.manifest = manifest;
    this.cache.dataDirectory = dataDirectory;
    this.cache.lastUpdate = new Date();
    // 必须在 records===[] 的成功分支返回之前输出这一行。
    this.outputChannel.appendLine(formatRefreshDiagnostic({
      trigger: request.trigger,
      filesDiscovered: manifest.entries.size,
      filesChanged: delta.changed.length,
      filesReused: delta.reused.length,
      filesRemoved: delta.removed.length,
      filesFailed: loaded.diagnostics.filesFailed,
      bytesRead: loaded.diagnostics.bytesRead,
      linesParsed: loaded.diagnostics.linesParsed,
      watcherEvents,
      coalescedTriggers,
      manifestMs,
      readParseMs: loaded.diagnostics.readParseMs,
      aggregateRenderMs,
      totalMs: performance.now() - totalStarted,
    }));
  } catch {
    // Scanner/reconciliation failures (EACCES/EIO included) are failed
    // attempts, never an empty corpus. Cache assignments occur only on the
    // successful paths above, so the previous snapshot remains authoritative.
    this.outputChannel.appendLine(formatRefreshDiagnostic({
      trigger: request.trigger,
      filesDiscovered: 0,
      filesChanged: 0,
      filesReused: 0,
      filesRemoved: 0,
      filesFailed: 1,
      bytesRead: 0,
      linesParsed: 0,
      watcherEvents,
      coalescedTriggers,
      manifestMs: 0,
      readParseMs: 0,
      aggregateRenderMs: 0,
      totalMs: performance.now() - totalStarted,
    }));
  } finally {
    const next = this.refreshGate.complete();
    if (next !== null) {
      setTimeout(() => void this.runRefresh(next), 0);
    }
  }
}
```

删除旧 `isRefreshing` / `pendingRefresh` / `pendingManual` 字段；`RefreshSingleFlight.pendingTrigger` 明确以 `null` 表示没有 follow-up，不能初始化成 `'poll'`。不得把 manifest 写入 `globalState`、磁盘或日志。loader 成功后，必须先完成 records/contentAnalysis/UI、保存 manifest、输出 diagnostics，才允许结束合法的空 records 路径。loader outer catch 必须令 `filesFailed >= 1`；scanner/loader 的任何非 `ENOENT` 失败都只产生匿名 failed diagnostic，保持旧 records/manifest/dataDirectory，不呈现 no-data，不提交 candidate manifest，并由下一 trigger 重试。

在 `src/test/refreshPolicy.test.ts` 增加不依赖 VS Code host 的 transaction-seam 测试：预置可识别的旧 manifest/records，让 scanner 抛 `EACCES`，断言 candidate commit callback 未调用且旧对象引用未变；随后让 scanner 成功，断言下一次 trigger 再次扫描且可提交。这样测试的是“失败不推进 baseline”，不是只测试 errno helper。

- [ ] **Step 4: 运行 GREEN、完整测试并人工核对 trigger call sites**

Run: `npm run compile && node --test out/test/refreshPolicy.test.js out/test/refreshDiagnostics.test.js out/test/claudeUsageFiles.test.js out/test/dataLoader.test.js`

Expected: PASS，0 failures。

Run: `rg -n "refreshData\(" src/extension.ts`

Expected: 只出现表中九个 call site 和方法定义；可见 `'startup'`、`'poll'`、`'credentials'`、`'watch'`、`'focus'`、`'workspace'`、`'settings'`、`'pricing'`、`'manual'` 各自的显式调用，不得残留无参数调用。

Run: `rg -n "needFullRefresh.*records\.length|records\.length === 0.*needFullRefresh|pendingTrigger:\s*RefreshTrigger\s*=\s*'poll'" src/extension.ts src/refreshPolicy.ts`

Expected: 无输出，退出码 1；合法空 corpus 由 non-null manifest 表示已经加载，pending trigger 由 `null` 表示不存在。

- [ ] **Step 5: 提交 extension 集成**

```bash
git add src/extension.ts src/refreshPolicy.ts src/test/refreshPolicy.test.ts
git commit -m "perf: coalesce watched refresh diagnostics"
```

### Task 5: 六语言 workaround、CHANGELOG 与发布验证

**Files:**
- Modify: `CHANGELOG.md:7`
- Modify: `README.md:302-305`
- Modify: `README-en.md:63-71`
- Modify: `README-zh-TW.md`
- Modify: `README-zh-CN.md:175-176`
- Modify: `README-ja.md`
- Modify: `README-ko.md`

**Interfaces:**
- Consumes: Task 1–4 的实际行为与诊断字段。
- Produces: 六语言一致的 #70 临时处理说明，以及只称为 mitigation 的 V2.2.1 CHANGELOG。

- [ ] **Step 1: 先写会失败的文档一致性检查**

Run:

```bash
for file in README.md README-en.md README-zh-TW.md README-zh-CN.md README-ja.md README-ko.md; do
  rg -q "300[–-]900" "$file" || exit 1
  rg -q "#70" "$file" || exit 1
done
```

Expected: FAIL；至少一份 README 尚无 Live refresh Off + 300–900 秒 workaround。

- [ ] **Step 2: 在六份 README 的 Troubleshooting 写入对应语言的精确说明**

`README.md` 与 `README-en.md` 使用：

```md
**High CPU or sluggish refresh on a large history (Linux included)**
- V2.2.1 removes the hidden 8-second active polling override and bounds the
  first-timestamp scan. Until you install it, set **Live refresh delay** to
  **Off**, set **Refresh interval** to **300–900 seconds**, and optionally turn
  **Content analysis** off. Turning Dashboard auto-refresh off by itself does
  not stop status-bar parsing.
- If V2.2.1 still runs hot, open **Show Diagnostic Logs** and attach only the
  anonymous `refresh:` lines to issue #70; they contain counts and timings, not
  prompts, paths, session IDs, credentials, or raw log lines.
```

`README-zh-CN.md` 使用：

```md
**大历史下 CPU 占用高或刷新缓慢（包括 Linux）**
- V2.2.1 移除了 active 状态下隐藏的 8 秒轮询覆盖，并限制首时间戳扫描。
  在安装 V2.2.1 前，可先把**实时刷新延迟**设为**关闭**，把**刷新间隔**设为
  **300–900 秒**，并视需要关闭**内容分析**。只关闭“仪表盘自动刷新”并不会
  停止状态栏所需的日志解析。
- 若 V2.2.1 仍持续高占用，请运行 **Show Diagnostic Logs**，只把匿名的
  `refresh:` 行附到 issue #70；其中只有计数和耗时，不含提示词、路径、
  session ID、凭证或原始日志行。
```

`README-zh-TW.md` 使用：

```md
**大量歷史資料下 CPU 佔用高或重新整理緩慢（包括 Linux）**
- V2.2.1 移除了 active 狀態下隱藏的 8 秒輪詢覆蓋，並限制首時間戳掃描。
  安裝 V2.2.1 前，可先把**即時重新整理延遲**設為**關閉**，把**重新整理間隔**
  設為 **300–900 秒**，並視需要關閉**內容分析**。只關閉儀表板自動重新整理
  並不會停止狀態列所需的日誌解析。
- 若 V2.2.1 仍持續高佔用，請執行 **Show Diagnostic Logs**，只把匿名的
  `refresh:` 行附到 issue #70；其中只有計數與耗時，不含提示詞、路徑、
  session ID、憑證或原始日誌行。
```

`README-ja.md` 使用：

```md
**履歴が大きい環境で CPU 使用率が高い／更新が遅い（Linux を含む）**
- V2.2.1 では active 時に隠れて 8 秒へ短縮されるポーリングを廃止し、最初の
  timestamp 読み取りを制限します。導入前は **Live refresh delay** を **Off**、
  **Refresh interval** を **300–900 秒**にし、必要なら **Content analysis** も
  Off にしてください。Dashboard auto-refresh だけを Off にしても、ステータス
  バー用の解析は止まりません。
- V2.2.1 でも高負荷が続く場合は **Show Diagnostic Logs** の匿名 `refresh:` 行だけを
  issue #70 に添付してください。prompt、path、session ID、credential、raw log は含みません。
```

`README-ko.md` 使用：

```md
**큰 기록에서 CPU 사용률이 높거나 새로 고침이 느림(Linux 포함)**
- V2.2.1은 active 상태에서 숨겨진 8초 폴링 강제를 제거하고 첫 timestamp 읽기를
  제한합니다. 설치 전에는 **Live refresh delay**를 **Off**, **Refresh interval**을
  **300–900초**로 설정하고 필요하면 **Content analysis**도 끄세요. Dashboard
  auto-refresh만 끄면 상태 표시줄용 로그 파싱은 계속됩니다.
- V2.2.1에서도 높은 사용률이 계속되면 **Show Diagnostic Logs**의 익명 `refresh:`
  줄만 issue #70에 첨부하세요. prompt, path, session ID, credential, raw log는 포함되지 않습니다.
```

- [ ] **Step 3: 在既有 V2.2.1 标题下追加准确、不夸大的条目**

前置 hygiene 计划已经创建唯一的 `## [2.2.1] — Unreleased`。不得再插入第二个版本标题；在该标题下保留既有 `### Changed`，再追加以下 `### Fixed`、`### Diagnostics` 和 gate 说明：

```md
### Fixed
- **High-CPU refresh mitigation (#70)** — polling now always honors the
  configured 30–3600 second `refreshInterval`; file watching is quiet-debounce
  only and adds 60/120/300-second choices. First-timestamp reads stop after the
  first valid timestamp and run with at most eight readers.

### Diagnostics
- **Anonymous refresh timings** — Show Diagnostic Logs now reports trigger,
  file/change/reuse/removal counts, bytes, parsed lines, watcher/coalescing
  counts, and manifest/read-parse/aggregate-render/total timings. It never logs
  prompts, paths, session IDs, credentials, or raw JSONL lines.

> This is a mitigation pending Linux prerelease validation. Issue #70 remains
> open until the reporter confirms the result; the per-file incremental index
> is tracked separately.
```

Run: `test "$(rg -c '^## \[2\.2\.1\] — Unreleased$' CHANGELOG.md)" -eq 1`

Expected: exit 0；V2.2.1 标题恰好一个，已发布的 V2.2.0 日期保持 `2026-07-07`。

- [ ] **Step 4: 运行六语言检查、完整测试和 package 内容检查**

Run:

```bash
for file in README.md README-en.md README-zh-TW.md README-zh-CN.md README-ja.md README-ko.md; do
  rg -q "300|300–900" "$file" || exit 1
  rg -q "#70" "$file" || exit 1
done
CCU_NODE=/Users/carl/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
"$CCU_NODE" node_modules/typescript/bin/tsc -p ./
"$CCU_NODE" --test out/test/*.test.js
```

Expected: README loop exit 0；compile 与完整测试 exit 0，0 failures（基线 114 tests，加上本计划新增测试）。普通开发机/CI 可用等价的 `npm test`。

Run: `CCU_NODE=/Users/carl/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node; "$CCU_NODE" node_modules/@vscode/vsce/vsce ls --no-dependencies`

Expected: exit 0；输出不含 `.github/`、`src/`、`out/test/`、`docs/`、`CLAUDE.md`、`AGENTS.md`、`.env`、`*.map`。

Linux prerelease 验证使用固定步骤，不上传原始日志：

```text
1. 安装 V2.2.1 prerelease VSIX，重载 Extension Host。
2. Live refresh delay 保持默认 2s，Refresh interval 分别验证 60s 与 300s。
3. 连续运行 Claude Code 10 分钟，并同时记录 extension-host CPU。
4. 运行 “Claude Code Usage: Show Diagnostic Logs”。
5. 只复制 refresh: 行到 issue #70；确认行内没有 path、prompt、session ID 或 JSONL。
6. 若仍高 CPU，采集 Extension Host CPU profile，并保持 #70 开放。
```

- [ ] **Step 5: 提交文档并记录 mitigation gate**

```bash
git add CHANGELOG.md README.md README-en.md README-zh-TW.md README-zh-CN.md README-ja.md README-ko.md
git commit -m "docs: document v2.2.1 CPU mitigation"
```

## 完成定义

- `refreshInterval=300` 时，无论 watcher 最近是否活动，poll 都恰好等待 300 秒。
- watcher 事件只重置 quiet timer；60/120/300 秒选项可选，默认仍是 2 秒。
- 一个实际刷新最多产生一个 pending follow-up；并发触发不会并行全盘读取。
- manifest 在一次刷新中只扫描一次，并复用于 loader。
- 只有 `ENOENT` 可作为扫描期消失竞态处理；`EACCES`/`EPERM`/`EIO`/未知 scanner error 与任何 loader failure 都保留旧 snapshot，下一 trigger 重试。
- 为排序读取首时间戳时，最多 8 个 reader 并发，并在首个合法 timestamp 后停止。
- 诊断只含枚举 trigger、数字计数与耗时；敏感字段不可进入类型或 formatter。
- 六份 README 与 CHANGELOG 明确这是 #70 的 mitigation；Linux 验证前不关闭 issue。
- `npm test`（或上述 bundled-node 等价命令）和本地锁定的 `@vscode/vsce ls` release gate 全部通过。
