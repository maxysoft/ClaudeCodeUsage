# v2.3.0 发布清理实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 独立交付精确版本提醒、默认开启的关闭开关、每周 Opus 选项退休，以及 v2.3.0 Codex 接手仓库规则。

**架构：** 把版本选择逻辑提取为无 VS Code 依赖的纯函数，`extension.ts` 只负责读取设置和显示通知。配额 formatter 只保留通用 5h／weekly 窗口；旧设置保持 inert。仓库 policy test 锁定双语规则、真实 Codex co-author 和文档同步要求。

**技术栈：** TypeScript 5.8、VS Code Extension API、`node:test`、现有 `SettingsStore`／`I18n`，无新增 runtime dependency。

## 全局约束

- 不手工修改 `package.json` 版本；release workflow 从 tag 写入版本。
- 所有用户可见文本覆盖 `en`、`de-DE`、`zh-TW`、`zh-CN`、`ja`、`ko`、`pt-BR`、`id`。
- `releaseAnnouncements` 默认 true；fresh install 不弹窗，关闭时仍记录已见版本。
- 当前通知只允许完整键 `2.3.0`，不得回退到 `2.2`。
- 删除 `showOpusWeekly` 当前功能面，但保留历史 changelog 和贡献者 credit。
- Claude 通用 5h／weekly quota 保持不变。
- 由 Codex 主要完成的 commit 带真实 OpenAI Codex co-author 与 footer。
- 每个任务 red-green TDD，任务结束时形成可独立审阅的提交。

---

## 文件结构

- 新建 `src/releaseAnnouncements.ts`：纯版本选择逻辑与 catalog contract。
- 新建 `src/test/releaseAnnouncements.test.ts`：完整版本、fresh install、关闭和 preview 测试。
- 修改 `src/extension.ts`：设置读取、localized v2.3.0 内容和通知 glue。
- 修改 `src/settings.ts`、`src/types.ts`、`src/i18n.ts`：新开关，删除 Opus 开关。
- 修改 `src/quotaFormat.ts`、`src/statusBar.ts`、`src/test/quotaFormat.test.ts`：删除 Opus UI 路径。
- 修改 `src/test/repositoryPolicy.test.ts`：锁定 v2.3.0 policy。
- 修改 `AGENTS.md`、`AGENTS.zh-CN.md`、`ARCHITECTURE.md`、`ARCHITECTURE-zh-CN.md`、`CHANGELOG.md`：双语规则与发布记录。

### 任务 1：精确版本提醒纯逻辑

**文件：**
- 新建：`src/releaseAnnouncements.ts`
- 新建：`src/test/releaseAnnouncements.test.ts`

**接口：**
- 产出：`ReleaseAnnouncementCatalog`、`announcementForUpgrade()`、`latestAnnouncementVersion()`。
- 消费方：任务 2 的 `extension.ts`。

- [ ] **步骤 1：写失败测试**

```ts
import { test } from 'node:test';
import * as assert from 'node:assert/strict';
import { announcementForUpgrade, latestAnnouncementVersion } from '../releaseAnnouncements';

const catalog = { '2.3.0': { version: '2.3.0' } };

test('upgrade resolves only the exact current version', () => {
  assert.deepEqual(announcementForUpgrade('2.3.0', '2.2.1', true, catalog), catalog['2.3.0']);
  assert.equal(announcementForUpgrade('2.3.1', '2.3.0', true, catalog), null);
  assert.equal(announcementForUpgrade('2.2.9', '2.1.0', true, catalog), null);
});

test('fresh install, already-seen, and disabled announcements do not show', () => {
  assert.equal(announcementForUpgrade('2.3.0', undefined, true, catalog), null);
  assert.equal(announcementForUpgrade('2.3.0', '2.3.0', true, catalog), null);
  assert.equal(announcementForUpgrade('2.3.0', '2.2.1', false, catalog), null);
});

test('preview selects the greatest full semantic version', () => {
  assert.equal(latestAnnouncementVersion({ '2.3.0': {}, '2.10.0': {}, '2.9.5': {} }), '2.10.0');
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npm run compile`
预期：FAIL，模块 `../releaseAnnouncements` 不存在。
注：红灯阶段因编译失败，无需再运行已编译产物。

- [ ] **步骤 3：实现最小纯函数**

```ts
export interface ReleaseAnnouncement {
  version: string;
}

export type ReleaseAnnouncementCatalog<T extends ReleaseAnnouncement = ReleaseAnnouncement> = Record<string, T>;

export function announcementForUpgrade<T extends ReleaseAnnouncement>(
  current: string,
  lastSeen: string | undefined,
  enabled: boolean,
  catalog: ReleaseAnnouncementCatalog<T>
): T | null {
  if (!enabled || !current || !lastSeen || current === lastSeen) return null;
  return catalog[current] ?? null;
}

export function latestAnnouncementVersion(catalog: Record<string, unknown>): string | undefined {
  const cmp = (a: string, b: string): number => {
    const av = a.split('.').map(Number);
    const bv = b.split('.').map(Number);
    for (let i = 0; i < Math.max(av.length, bv.length); i++) {
      const d = (av[i] ?? 0) - (bv[i] ?? 0);
      if (d !== 0) return d;
    }
    return 0;
  };
  return Object.keys(catalog).sort(cmp).pop();
}
```

- [ ] **步骤 4：运行聚焦测试确认通过**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/releaseAnnouncements.test.js`
预期：3 tests PASS。

- [ ] **步骤 5：提交**

```bash
git add src/releaseAnnouncements.ts src/test/releaseAnnouncements.test.ts
git commit -m "fix: scope release announcements to exact versions"
```

### 任务 2：接入通知开关和 v2.3.0 本地化内容

**文件：**
- 修改：`src/settings.ts`
- 修改：`src/types.ts`
- 修改：`src/i18n.ts`
- 修改：`src/extension.ts`
- 修改：`src/test/repositoryPolicy.test.ts`

**接口：**
- 消费：任务 1 的 `announcementForUpgrade()`、`latestAnnouncementVersion()`。
- 产出：`ExtensionConfig.releaseAnnouncements: boolean` 和八 locale 的 v2.3.0 文案。

- [ ] **步骤 1：先扩展 policy test**

```ts
test('release announcements are exact-version and user-disableable', () => {
  const extension = repoFile('src/extension.ts');
  const settings = repoFile('src/settings.ts');
  assert.match(extension, /'2\.3\.0'/);
  assert.doesNotMatch(extension, /'2\.2'\s*:/);
  assert.match(settings, /key:\s*'releaseAnnouncements'/);
  assert.match(settings, /default:\s*true/);
});
```

- [ ] **步骤 2：运行确认失败**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/repositoryPolicy.test.js`
预期：FAIL，仍存在 `2.2` catalog 且无 `releaseAnnouncements`。

- [ ] **步骤 3：添加设置 contract**

在 `SETTINGS` general group 添加：

```ts
{
  key: 'releaseAnnouncements',
  type: 'boolean',
  default: true,
  storage: 'state',
  group: 'general',
  label: 'Release announcements',
  help: "Show one What's New notification after an extension upgrade.",
},
```

在 `ExtensionConfig` 添加 `releaseAnnouncements: boolean`，在 `getConfiguration()` 返回 `s.get<boolean>('releaseAnnouncements')`。

- [ ] **步骤 4：把 notification glue 改为完整版本**

`extension.ts` 的 catalog 使用 `version` 和 localized body factory；`maybeAnnounceWhatsNew()` 必须先写 `ccu.lastSeenVersion`，再用任务 1 纯函数决定是否显示：

```ts
const announcement = announcementForUpgrade(
  current,
  last,
  this.getConfiguration().releaseAnnouncements,
  WHATS_NEW
);
void this.context.globalState.update('ccu.lastSeenVersion', current);
if (announcement) this.showWhatsNew(announcement.version, announcement.body());
```

`previewWhatsNew()` 用 `latestAnnouncementVersion(WHATS_NEW)`，不得读取 major.minor。

- [ ] **步骤 5：为八 locale 添加 v2.3.0 标题、正文和设置文案**

英文正文严格只包含：Codex Beta、local optimization guidance、correct version announcements、weekly Opus cleanup。其他 locale 忠实翻译；设置面板通过 `SETTINGS_I18N` 覆盖 `releaseAnnouncements`。

- [ ] **步骤 6：运行聚焦和完整测试**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/releaseAnnouncements.test.js out/test/repositoryPolicy.test.js`
预期：PASS。
运行：`npm test`
预期：现有 suite PASS，只有既有 filesystem skip。

- [ ] **步骤 7：提交**

```bash
git add src/settings.ts src/types.ts src/i18n.ts src/extension.ts src/test/repositoryPolicy.test.ts
git commit -m "feat: announce only the current release"
```

### 任务 3：退休每周 Opus 开关

**文件：**
- 修改：`src/quotaFormat.ts`
- 修改：`src/statusBar.ts`
- 修改：`src/extension.ts`
- 修改：`src/settings.ts`
- 修改：`src/types.ts`
- 修改：`src/test/quotaFormat.test.ts`
- 修改：`src/test/repositoryPolicy.test.ts`

**接口：**
- `QuotaStatusOptions` 只保留 `showReset`、`fiveHourOnly`、`resetFormat`、`now`。
- `StatusBarManager.setVisibility()` 不再接受 `showOpusWeekly`。

- [ ] **步骤 1：先把 quota 测试改成新 contract**

```ts
test('weekly formatter ignores retired model-specific windows', () => {
  const s = formatQuotaStatusText(live, { showReset: false, fiveHourOnly: false, now: NOW });
  assert.equal(s, '5h 6% · wk 1%');
});

test('worst utilisation uses only generic windows', () => {
  assert.equal(worstShownUtilisation(live, { showReset: false, fiveHourOnly: false }), 6);
});
```

- [ ] **步骤 2：添加 repository absence test 并确认失败**

```ts
test('retired showOpusWeekly is absent from active product code', () => {
  for (const file of ['src/settings.ts', 'src/types.ts', 'src/extension.ts', 'src/statusBar.ts', 'src/quotaFormat.ts']) {
    assert.doesNotMatch(repoFile(file), /showOpusWeekly/, `${file} still exposes showOpusWeekly`);
  }
});
```

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/quotaFormat.test.js out/test/repositoryPolicy.test.js`
预期：FAIL，旧 option 仍存在。

- [ ] **步骤 3：删除 formatter 与 status-bar 分支**

`LiveQuotaWindows` 可继续容忍 source payload 中的未知字段，但 formatter 只读取 `five_hour`／`seven_day`。删除 `showOpusWeekly` option、`opus` segment 和 worst-utilisation 分支；同步删除 `StatusBarManager` field／parameter 及所有调用参数。

- [ ] **步骤 4：删除 setting/config mapping，保留旧 state inert**

从 `SETTINGS` 和 `ExtensionConfig` 删除 `showOpusWeekly`；不调用 `globalState.update(..., undefined)`，从而不做破坏性 migration。

- [ ] **步骤 5：运行聚焦和完整测试**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/quotaFormat.test.js out/test/repositoryPolicy.test.js`
预期：PASS。
运行：`npm test`
预期：PASS。

- [ ] **步骤 6：提交**

```bash
git add src/quotaFormat.ts src/statusBar.ts src/extension.ts src/settings.ts src/types.ts src/test/quotaFormat.test.ts src/test/repositoryPolicy.test.ts
git commit -m "refactor: retire the weekly Opus quota option"
```

### 任务 4：更新 v2.3.0 双语仓库规则

**文件：**
- 修改：`AGENTS.md`
- 修改：`AGENTS.zh-CN.md`
- 修改：`ARCHITECTURE.md`
- 修改：`ARCHITECTURE-zh-CN.md`
- 修改：`CHANGELOG.md`
- 修改：`src/test/repositoryPolicy.test.ts`

**接口：**
- 产出：后续 provider/index/UI 任务必须遵守的 canonical policy。

- [ ] **步骤 1：先更新 policy test 的预期**

```ts
assert.match(agents, /Codex Beta in v2\.3\.0/);
assert.match(agents, /2\.4-GB-class history.*worker/i);
assert.match(agents, /Co-authored-by: OpenAI Codex <215057067\+openai-codex\[bot\]@users\.noreply\.github\.com>/);
assert.match(chinese, /v2\.3\.0.*Codex Beta/);
assert.match(chinese, /主要撰写.*OpenAI Codex.*Co-authored-by/is);
```

- [ ] **步骤 2：运行确认失败**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/repositoryPolicy.test.js`
预期：FAIL，policy 仍为 v2.2.1 Claude-only 且禁止真实 co-author。

- [ ] **步骤 3：同步更新两份 AGENTS**

写明：v2.3.0 是 Claude + Codex Beta；Codex local-only 数据源；worker/incremental index；八 locale／七 README；禁止 credential；comment footer；真实 Codex co-author；贡献者 PR 保留规则；未经明确许可不得 push／PR／merge／release。

- [ ] **步骤 4：同步更新两份 ARCHITECTURE 和 CHANGELOG**

ARCHITECTURE 添加 provider flow、Codex token subset 语义、worker/index 和 last-observed limit；移除“Claude-only/out-of-scope multi-provider”的当前描述。CHANGELOG 新增 `## [2.3.0] — Unreleased`，记录本计划交付，不改 2.2.1 历史。

- [ ] **步骤 5：验证文档与 policy**

运行：`npm run compile`
预期：PASS。
运行：`node --test out/test/repositoryPolicy.test.js`
预期：PASS。
运行：`git diff --check`
预期：无输出。
运行：`git ls-files --stage`
预期：新建和修改的文本文件均显示 mode `100644`。

- [ ] **步骤 6：提交（带 Codex 署名）**

```bash
git add AGENTS.md AGENTS.zh-CN.md ARCHITECTURE.md ARCHITECTURE-zh-CN.md CHANGELOG.md src/test/repositoryPolicy.test.ts
git commit -m "docs: prepare repository policy for Codex beta"
```

## 计划完成检查

- [ ] `rg -n "showOpusWeekly|'2\.2'\s*:" src` 仅允许历史注释／无活动命中。
- [ ] `releaseAnnouncements=false`、fresh install、2.3.1 无 catalog 三条路径均有测试。
- [ ] `npm test` 通过。
- [ ] 工作树只包含本计划明确文件。
